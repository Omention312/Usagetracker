# UsageTracker UI 风格规范(源自本机 `参考\` 目录的截图分析;该目录为设计参考,未入库)

> 分析方式:像素扫描 + OCR + 调色板(模型不支持直接读图,已用工具多角度提取)。
> 参考图内容:1.png=FluentFlyout 深色托盘菜单;2.png=Clash Verge(深色)首页;3.png=Clash Verge(深色)设置页。

## 1. 主窗口(参考 Clash Verge 深色仪表盘)

| 令牌 | 值 | 用途 |
|---|---|---|
| --bg | `#2a2c38` | 主内容背景(冷调深灰蓝) |
| --sidebar | `#1e1f27` | 左侧导航栏背景 |
| --surface | `#283245` | 卡片/列表行背景(略亮冷调) |
| --surface-hover | `#343b4d` | 悬停层 |
| --accent | `#178bff` | 主强调(选中/按钮/链接/进度) |
| --accent-2 | `#0a84ff` | 辅助强调 |
| --border | `rgba(255,255,255,0.06)` | 细分隔 |
| --text | `#f2f3f7` | 主文字 |
| --text-dim | `#9a9ca8` | 次级文字/说明 |
| --danger | `#e5484d` | 黑名单/危险操作 |
| --ok | `#30d158` | 运行中绿点等 |

深色正文 ≥4.5:1;浅色模式同构映射(浅底 #f7f7fa、surface #fff、text #1c1d22、accent #0a84ff)。

## 2. 布局骨架(复刻 2/3.png)

```
┌──────────┬──────────────────────────────────────────┐
│ 侧栏导航  │ 页面标题(22px semibold)                  │
│ 宽 ~200px │ ┌ 统计头(开始统计时间…)                ┐  │
│ #1e1f27  │ │ 内容卡片/列表区域(可滚动)            │  │
│ 图标+文字 │ │  每行: [图标] 名称 ... 时长 [黑名单] │  │
│ 选中=     │ └──────────────────────────────────────┘ │
│ 高亮胶囊   │ 页面间切换仅换内容区                     │
└──────────┴──────────────────────────────────────────┘
```
- 导航条目:高 ~40px,行内 8px 间隔;选中态 = 胶囊底 `#2a3curva` + 左侧 3px 强调条(参考侧栏青色竖条);悬停 10% 强调。
- 卡片圆角 10px、无重阴影(细边框),留白 16-20px。

## 3. 托盘/任务栏菜单(参考 1.png FluentFlyout)

- 弹出层:深灰 `#303030`、圆角 10px、细边框、无分割线。
- 条目:高 ~36px、左图标(24px)+文字;悬停 = 浅灰胶囊(`#4f4f4f`、圆角 8px、左右留白 ~8px)。
- 定位:显示在托盘图标上方/鼠标附近(Shell_NotifyIconGetRect),不再固定屏幕右下角。

## 4. 字体层级(Microsoft YaHei UI)

| 层级 | 尺寸 | 字重 |
|---|---|---|
| 页面标题 | 20-22px | SemiBold |
| 章节/卡片标题 | 15-16px | SemiBold(大写感弱化,中文) |
| 正文/条目 | 13-14px | Regular |
| 次要/脚注 | 12px | Regular |
| 数字时长 | 14px | Medium(等宽观感 Segoe UI) |

## 5. 组件

- 按钮:胶囊/圆角8px,主按钮=accent 底白字;次按钮=surface 底 + border。
- 开关:参照深色系统开关(accent 亮起、灰轨熄灭)。
- 分段选择(主题):浅灰胶囊容器内三枚(参考 3.png “浅色/深色/系统”)。
- 列表行:卡片内每行 = icon 32px + 名称 + 时长 + 操作;hover 提亮;右侧黑名单按钮文字随状态(加入/取消)。

## 6. 页面组织(映射需求 3.1/6/7/8)

四个页面:今日概览 / 全部应用概览 / 黑名单 / 设置。每页顶部显示“开始统计时间:YYYY-MM-DD HH:mm:ss”。
